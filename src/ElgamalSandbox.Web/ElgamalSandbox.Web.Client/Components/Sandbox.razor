@using System.Text.Json.Nodes
@using ElgamalSandbox.Components.Sandbox
@rendermode renderMode

<SandboxCanvas 
    ToolBox="@ToolBox"></SandboxCanvas>


@code {
    private static InteractiveWebAssemblyRenderMode renderMode = new(false);

    [Parameter] public JsonObject ToolBox { get; set; }
}
